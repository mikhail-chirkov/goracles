apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}      
      targetPort: {{ .Values.api.containerPort }}
      protocol: TCP
  selector:
    app: {{ .Release.Name }}-api